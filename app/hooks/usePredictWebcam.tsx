import * as cocoSsd from "@tensorflow-models/coco-ssd";

const usePredictWebcam = (model: cocoSsd.ObjectDetection) => {
  let predictions = [];
  return predictions;
};

export default usePredictWebcam;
